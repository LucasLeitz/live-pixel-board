services:
  frontend:
    build: ./frontend
    container_name: pixelboard-frontend
    ports:
      - "4200:4200"
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: pixelboard-backend
    ports:
      - "8080:8080"